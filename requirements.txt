# Requirements for ResQ

# Core dependencies
openai>=1.0.0
python-dotenv>=1.0.0
pytest>=7.0.0
pytest-asyncio>=0.23.0
flask>=3.0.0
requests>=2.31.0
psutil>=5.9.0
rich>=13.0.0
textual>=0.40.0

# Alibaba Cloud SLS (Simple Log Service) — official Python SDK.
# Optional: only needed for `python main.py --sls-incident` (live SLS log reads).
# The target service has its own requirements (SLS/OSS/ECS/CMS) — see target-service/.
aliyun-log-python-sdk>=0.9.0
